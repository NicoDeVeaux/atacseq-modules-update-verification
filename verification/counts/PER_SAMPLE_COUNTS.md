# Per-sample consensus counts: dev vs modules-update branch

Consensus featureCounts read counts for the **HIV MDDC (GRCh38)** cohort, compared
between upstream `dev` and the `nf-core-modules-update` branch. Generated by
`../scripts/compare_featurecounts.py` from the committed count matrices under
`dev/` and `branch/`. Totals are summed over the **69,596 consensus intervals
shared by exact coordinate** between the two arms.

## Commit hashes (what was compared)

| Arm | Meaning | featureCounts version (from file header) | Commit |
|-----|---------|------------------------------------------|--------|
| dev | upstream `nf-core/atacseq:dev` | **v2.0.1** | baseline |
| branch | `NicoDeVeaux/atacseq:nf-core-modules-update` | **v2.1.1** | run at `0116142` (tree-identical to the tested `52836f0`) |

PRs under review (current head SHAs at time of writing — re-fetch before relaunch):

| PR | Branch | Head SHA |
|----|--------|----------|
| #448 nf-core modules update | `nf-core-modules-update` | `5caa50bfa325daa9af6feede2d3b4cf45b456d5e` |
| #452 mixed SE/PE per-batch counting | `fix/featurecounts-mixed-se-pe` | `755632c17a94e034dda0b0e1e9b92213c325c84c` |

## Per-sample totals

| Sample | dev total | branch total | delta | ratio | Pearson r |
|--------|----------:|-------------:|------:|------:|----------:|
| HIV_CD86HI_48H_REP1 | 755,472 | 1,445,223 | +689,751 | 1.91× | 0.999411 |
| HIV_CD86HI_48H_REP2 | 1,049,186 | 2,018,283 | +969,097 | 1.92× | 0.999593 |
| MOCK_48H_REP1 | 728,326 | 1,391,601 | +663,275 | 1.91× | 0.999291 |
| MOCK_48H_REP2 | 1,207,245 | 2,327,082 | +1,119,837 | 1.93× | 0.999567 |

Consensus interval count: dev **74,895**, branch **74,935** (69,596 shared by exact
coordinate; 5,299 dev-only, 5,339 branch-only — mostly a few-bp boundary shifts).

## What this means

Every sample's counts **roughly double** (consistent ~1.92×), while Pearson r ≈ 0.9996
shows a near-uniform proportional scaling rather than random change. Both arms call
featureCounts with identical flags (`-F SAF -O --fracOverlap 0.2 -p -s 0`) and neither
passes `--countReadPairs`.

**Most likely cause — the subread `-p` semantics change.** In subread **< 2.0.2**
(dev's v2.0.1) `-p` counts *fragments* (read pairs). From subread **2.0.2 onward**
(branch's v2.1.1) `-p` only declares paired-end input and counts *individual reads*
unless `--countReadPairs` is added. The ~1.92× inflation matches counting both mates
instead of one fragment.

This is an **inference** from the version boundary plus the 2× signature; it was not
confirmed by re-running featureCounts. See `README.md` for the full write-up.

> **Reconcile with `../comparison.tsv`.** That table reports the `mLb.clN` consensus as
> 74,895 for *both* arms and describes the HIV MDDC run as bit-identical. The count
> matrices committed here show branch = 74,935 intervals and ~2× counts. The peak/FRiP
> numbers in `comparison.tsv` come from MultiQC; these counts come from the featureCounts
> matrices. Before treating either as final, confirm both sets of numbers were captured
> from the same branch run (`0116142` vs the current PR head `5caa50bf`).
